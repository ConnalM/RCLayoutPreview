using System.Windows.Media;

namespace RCLayoutPreview.Helpers
{
    public static class PlayerColors
    {
        public static readonly Color[] Colors = new Color[]
        {
            Color.FromRgb(255, 140, 0),      // Orange
            Color.FromRgb(255, 255, 0),      // Yellow
            Color.FromRgb(0, 191, 255),    // Light Blue
            Color.FromRgb(255, 255, 255),    // White
            Color.FromRgb(255, 215, 0),      // Bright Gold (Goldenrod)
            Color.FromRgb(0, 191, 255),      // Bright Light Blue (Deep Sky Blue)
            Color.FromRgb(124, 252, 0),      // Bright Light Green (Lawn Green)
            Color.FromRgb(255, 140, 0)       // Bright Orange (Dark Orange)
        };
    }
}
