<Window x:Class="RCLayoutPreview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RCLayoutPreview"
        Title="Preview"
        Height="600"
        Width="800"
        FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">
            <CheckBox x:Name="DebugModeToggle" Content="Diagnostics Mode" Margin="10,0,0,0" VerticalAlignment="Center" 
                      Checked="DebugModeToggle_Changed" Unchecked="DebugModeToggle_Changed"/>
            <TextBlock x:Name="StatusLabel" Text="Ready" Foreground="Gray" Margin="10,4,0,4" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Preview Panel -->
        <ScrollViewer Grid.Row="1" x:Name="PreviewScrollViewer" VerticalScrollBarVisibility="Auto" Margin="10">
            <Border x:Name="PreviewPanel" BorderBrush="#FFD0D0D0" BorderThickness="1" Padding="10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFF5F5F5" Offset="0"/>
                        <GradientStop Color="White" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <ContentControl x:Name="PreviewHost"/>
            </Border>
        </ScrollViewer>

        <!-- Popup overlay -->
        <Grid x:Name="PopupOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#80000000" Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Background="White" Margin="10">
                <TextBlock x:Name="PopupMessage" Text="" TextWrapping="Wrap" Margin="10" FontSize="14" Foreground="Red"/>
                <Button Content="OK" Width="100" HorizontalAlignment="Center" Click="PopupOkButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>